{# empty Twig template #}
{%extends "/layout/base.twig"%}
{% block title %}
	<title>Semio labs || Veuilez vous identifier </title>
{% endblock %}
{%block css%}
{{parent()}}

{%endblock%}
{%block content%}
<div id="wrap" class="wrap">
         
    <div class="content">
        
        
        <form class="formulaire" action="#" method="post" {{ form_enctype(form) }} novalidate>
                  {% if app.session.hasFlash('success') %}
        <div class="alert alert-block alert-success">
                {{ app.session.flash('success') | raw }}
        </div>
            {% elseif app.session.hasFlash('error') %}
        <div class="alert alert-block alert-error">
            <h4>Error</h4>
            {{ app.session.flash('error') | raw }}
        </div>
            {% endif %} 
                    {{ form_widget(form) }}
                    
                    <input type="submit" name="Ajouter" class="btn btn-large btn-primary" />
               </form>
 </div>
    </div>
{% endblock %}

